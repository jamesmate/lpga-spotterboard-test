// Deprecated: superseded by Hole.tsx + HoleRow.tsx (inline expand/collapse
// with HoleMap, no modal). Kept only because this synced folder doesn't
// allow file deletion from the agent; safe to delete manually.
export {};
