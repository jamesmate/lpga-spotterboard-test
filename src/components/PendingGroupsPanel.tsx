// Deprecated: the waiting-groups lists are now embedded inline in
// HoleWithQueue.tsx (rendered as the first item of each nine's HoleRow),
// whose header doubles as the header for the scrollable queue. Kept only
// because this synced folder doesn't allow file deletion from the agent;
// safe to delete manually.
export {};
